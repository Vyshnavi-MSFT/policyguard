// mocks/findings.ts — Person B
// Fake Finding[] data so the dashboard/approval UI is never blocked by the backend.
// TODO: export a sample array of findings shaped like the shared Finding contract.
