// api/client.ts — Person A
// Typed API client for the PolicyGuard backend.
// TODO: implement startScan(), getScan(id), listPolicies(), approveFinding(id), downloadReport(id).
// Also define the TypeScript `Finding` type matching the shared backend contract.
