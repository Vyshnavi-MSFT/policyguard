// Models/Finding.cs — Person E (SHARED CONTRACT)
// public record Finding(Id, SourceType, Location, Snippet, DataType, Severity,
//   PolicyClauseId, PolicyClauseText, Explanation, ProposedFix, DetectedBy, Status)
// TODO: implement the shared Finding model everyone codes against.
